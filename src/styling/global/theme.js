import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    background: {
      default: '#000',
    },
    text: {
      primary: '#FFF',
    },
  },
});

export default theme;
